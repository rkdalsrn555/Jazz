import { themeProps } from "@emotion/react";

type btnProps = {
  content: string;
  color: string;
  img: string;
  width: string;
  height: string;
  destination: string;
};

type innerContainerProps = {
  width: string;
  height: string;
  theme: themeProps;
};
